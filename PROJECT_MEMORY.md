# 玄衡项目记忆

> 本文件是项目长期上下文。任何后续开发、上下文压缩或新会话开始时，应先阅读本文件，再查看代码。每完成一个里程碑、改变产品范围或发现新的阻塞项，都要同步更新“当前状态”和“下一步”。不要在此记录 API Key、用户出生资料或其他敏感数据。

## 1. 产品定位

- 产品名：玄衡。
- 当前核心：以确定性计算为基础的八字排盘、命盘研判、岁运时间轴和具体问事。
- 后续大版本：V2 加入塔罗；V3 加入紫微斗数。
- 产品原则：计算与 AI 解读分离。历法、四柱、大运、流年、神煞等必须由确定性引擎计算；AI 只解释已有结果，不反向修改命盘。
- 表达边界：传统文化研究与娱乐参考；不替代医疗、法律、投资或其他现实专业判断；避免恐吓式和宿命式断语。

## 2. 已确定的产品与交互方向

- 主页和功能页分开，主页只承担品牌、功能说明和功能入口。
- 仅保留顶部导航，不使用底部功能导航。
- “玄衡”Logo 是独立主页入口；功能导航与主页入口互相独立。
- 当前功能导航：命盘研判、岁运时间轴、问一件事；用户档案必须单独一页。
- 洞见/使用说明、设计理念各自独立成页；内容未确定时可以保留空白，等待共同讨论。
- 合缘功能保留“待开放”状态，不在 V1 实现。
- 出生资料编辑采用紧凑弹窗：顶部阳历/阴历/四柱标签；阳历和阴历模式下为左右等宽双列。
- 左列：名称、日期时间滚轮、性别；右列：出生城市、地图、时区、北京时间/真太阳时。
- 地区输入和地图是唯一一套定位来源；真太阳时复用同一经纬度，不设置第二个重复定位入口。
- 地图支持选点、设备定位、全屏和 Esc 退出。
- 阳历、阴历、四柱必须由同一排盘引擎双向换算并保持同步。
- 阴历日期显示中国传统写法：初一至初十、十一至二十、廿一至廿九、三十，并正确处理闰月。
- 四柱反查允许同一组四柱匹配多个出生日期，由用户选择候选日期。
- 历史资料自动去重；同一出生资料不重复建立记录；历史可搜索、编辑和单条删除。
- “编辑资料”必须携带原档案 ID 并原地更新，名称变化不能被当成新档案；“新增资料”必须清空编辑 ID。
- 已生成分析应缓存保存，重新打开页面不重复调用模型。
- API Key 不应要求用户每次填写。当前本地版使用 macOS 钥匙串保存。
- 视觉方向参考 Refero 提供的样式和 MetaSight/观象的布局逻辑，但不直接照搬品牌资产。
- 首页主视觉不再使用“观命见时，问事见行”八字口号，直接展示产品名“玄衡”；产品名按 Logo/品牌位管理，后续更名优先替换统一品牌资产，而不是逐页修改主标题。

## 3. 版本路线

### V1.x — 八字正式产品

#### V1.0.0 基础闭环

- 三种录入方式与同步换算。
- 地区、地图、时区和真太阳时。
- 确定性四柱、大运、流年、神煞计算。
- 历史保存、自动去重、编辑和删除。
- 已生成分析缓存。

#### V1.1.0 命盘研判

- 四柱、藏干、十神、纳音、五行。
- 日主旺衰、格局、喜忌和神煞。
- 性情、事业、关系等结构化解读。
- 明确区分计算结果与 AI 解读。

#### V1.2.0 岁运时间轴

- 大运顺逆、起运年龄和当前大运定位。
- 大运与流年分层展示。
- 点击阶段查看结构、变化和行动参考。

#### V1.3.0 问一件事

- 必须基于一份已有命盘。
- 带入原局、当前大运、流年和问题时间范围。
- 输出条件、阻力、时间窗口和行动建议。
- 问事记录保存并避免重复生成。

#### V1.4.0 数据与账户

- 从仅本机 SQLite 过渡到可部署的持久化数据库。
- 命盘、分析、问事分层保存。
- API Key 和模型配置只存在服务端环境变量。
- 匿名本地使用与登录后云同步的边界另行确定。

#### V1.5.0 正式上线验收

- 功能前端、后端和数据库均可在线访问。
- 桌面端和移动端适配。
- 错误、加载、空状态和重试完整。
- 排盘边界测试、隐私说明、免责声明、日志与备份。
- 至少 20 组真实资料人工复核。

### V2.x — 塔罗

- V2.0：接入 `daman-ovo-0404/tarot-skill` 的 78 张牌、正逆位、随机脚本与基础牌阵。
- V2.1：洗牌、抽牌、翻牌、牌面展示和占问历史。
- V2.2：牌间关系、综合叙事和全部六种牌阵。
- V2.3：移动端体验、安全边界、免费/付费规则与上线验收。
- 塔罗抽牌结果必须保存 seed 和 time factor，同一次占问可复现，不得重新打开时偷偷重抽。
- 上游 README 声称 MIT；当前取得提交没有独立 `LICENSE` 文件。正式公开发布前需确认许可，并持续保留来源说明。

### V3.x — 紫微斗数

- 参考 `FANzR-arch/Numerologist_skills/ziwei-doushu`。
- V3.0：先实现确定性排盘引擎，包括命身宫、十二宫、五行局、十四主星、辅煞、四化和大限。
- V3.1：十二宫命盘与宫位详情页。
- V3.2：三方四正、六个核心宫位与其余宫位解读。
- V3.3：本命、大限、流年四化分层时间轴。
- V3.4：至少 30 张权威软件命盘对照、边界测试和正式发布。
- 当前上游紫微仓库主要是规则知识库；`ziwei_verify.py` 只验证紫微星安星表，不是完整排盘引擎，不能直接作为生产后端。
- 当前上游仓库根目录未发现明确 LICENSE；直接复制代码或大段规则前必须确认许可。

## 4. 当前技术状态（2026-09-03）

### 当前版本判断

**V1.0 + V2.0 本地 RC 等待联合验收；V3.1 紫微校盘版已形成后端—档案—十二宫—运限前端闭环，等待人工验收与 30 盘对照；上线与 D 类验收未开始。**

### 已完成或已有实现

- `index.html`：完整的本地交互原型，包含主页、命盘、岁运、问事、用户档案和资料编辑页面。
- `server.py`：本地 API 服务、地区查询、阴阳历换算、四柱反查、八字排盘、分析接口、历史接口。
- `vendor/bazi-skill`：来源为 `https://github.com/jinchenma94/bazi-skill.git`，当前已知版本 `112a5d8`。
- 阳历、阴历、四柱滚轮和同步换算已有实现。
- 地图、时区、北京时间/真太阳时及经纬度校正已有实现。
- 地图全屏和 Esc 退出已有实现。
- 本地 SQLite：`.xuanheng/state.sqlite3`，包含命盘历史与分析缓存。
- 命盘历史使用 fingerprint 去重，支持搜索、加载、编辑和删除。
- 2026-09-02 已修复“未命名档案改名后新增一条记录”的身份链路：编辑时按原档案 ID 更新，若与旧版本遗留的完全相同指纹冲突则合并；新增入口会清空编辑 ID。已通过隔离 SQLite 回归测试，并在验收日志中多次出现 `history_record_edited` 成功事件。
- API Key 使用 macOS 钥匙串保存，公共模型配置保存在本地数据库。
- 最近一次验证：Python 语法通过、页面脚本语法通过、八字排盘 11 项自动测试全部通过。
- 已加入本地验收事件日志：关键操作写入 SQLite `acceptance_events`，终端实时输出 `[acceptance]` 事件，并可通过 `/api/acceptance-report` 汇总为“已通过／尝试失败／尚未测试”和加权完成度。
- 验收日志新增 `history_record_edited`，专门确认编辑保存是否原地更新，避免把普通指纹去重误判成“编辑成功”。
- 验收日志不记录姓名、出生日期、地点、经纬度、问题正文或 API Key；滚轮逐次滑动等高频体验操作不采集。
- 2026-09-02 用户确认第二版岁运预览，已接入正式 `#/timing` 页面：保留原“大运走势”，移除旧“三张年度提示”，新增“本大运流年走势”和“流月走势”。流年默认显示从当前年起的五年，可“展开/收起”当前大运完整十年并点击切换；流月默认显示当前所在的六个月组，可“展开/收起”全年十二流月并点击查看交接时间。
- `server.py` 新增 `/api/timing-data`：流年干支调用同一排盘引擎，流月从当年立春到次年立春按定气十二节生成，精确到分钟，不按公历月初。已核对 2026 丙申月为 8月7日 19:38—9月7日 22:40，戊戌月为 10月8日 14:28—11月7日 17:49。
- 岁运正式模块新增验收事件：定气数据载入、十年展开、流年切换、十二流月展开和流月详情点击；仍不采集具体出生资料。
- 2026-09-02 “问一件事”首页已改为居中轻量工作台：当前命盘档案名作为页面中央大标题，问事模式、输入区和推荐问题围绕档案名排列；未命名档案明确显示“未命名资料”，不沿用上一份档案名称。
- 从档案点击“查看报告”现为纯读取排盘，不再触发历史保存、去重或合并；只有新增或编辑提交才允许改变档案记录。
- “问一件事”工作台的随缘、看运、倾听、问事已接入同一 LLM 分流链路：切换模块会更新推荐问题、输入提示与回答结构；问事自由输入会自动识别岁运或倾听意图。看运会先调用确定性岁运/定气流月数据并要求列出依据，倾听只做人格式理解而不预测吉凶，随缘可在留空时随机选择命盘主题。
- 问答结果统一展示结论、正文、三条命盘/岁运依据、可选时间窗口与现实建议；随缘和倾听不展示时间窗口。新增 `question_intent_selected` 验收事件，日志只记录意图类型，不记录问题正文。
- 截至 2026-09-02，验收日志已有以下强语义成功链路：阳历同步三历、城市检索、设备定位、真太阳时排盘、确定性排盘、档案创建/查看/原地编辑/去重/删除、分析生成与缓存复用、岁运定气数据载入、十年与十二流月展开、流年和流月选择、问事完成，以及随缘/看运/倾听模式切换。后续人工清单不再重复这些基础操作。
- 2026-09-02 完成全路由视觉审查：当前共用同一配色与组件基础，但已分化为品牌展示、数据管理、分析报告、问事工作台四种视觉气质；全局导航、当前档案上下文、空状态、术语、字体角色和交互状态仍需统一。
- 2026-09-02 已在 `previews/` 完成三套视觉统一交互稿，均覆盖同一组 10 个页面/状态并共享同一信息架构与交互逻辑：A“雾白观象”（现有雾白淡紫方向）、B“单色薄荷”（Refero/Mintlify 的纯白、黑字、单一薄荷绿与全站无衬线体系）、C“玄夜星图”（深墨与金色功能强调）。三套均统一了页面宽度、留白节奏、导航、档案上下文、空/有数据状态、术语、按钮语义和线性图标；正式 `index.html` 尚未改动，等待用户选择或组合方向后再实施。
- 2026-09-02 新增 `previews/visual-system-c-expansion.html`，用于评审玄夜星图未来接入塔罗与紫微的导航接口。当前探索方案是“全局选择研判体系、体系内显示自己的功能导航”：Logo 后设置四柱八字/塔罗占问/紫微斗数切换器；八字与紫微共享人物出生档案但分别排盘，塔罗只保存占问与可复现抽牌记录；“问一件事”共用交互外壳，但每次必须明确选择一种研判依据。该方案仅为交互提案，尚未成为正式产品决策。
- 2026-09-03 用户确认首页品牌表达收敛：移除“观命见时，问事见行”八字主标题，正式页与三套视觉稿首页均改为直接展示产品名“玄衡”；未来更名通过统一 Logo/品牌位处理。
- 2026-09-03 排查“页面可打开但无法新建或读取档案”：验收日志最后停在 2026-09-02，故障发生时本机 `127.0.0.1:4173` 没有后端进程；静态 `index.html` 虽能显示，但档案、排盘和模型接口均不可用。已重新启动 `server.py`，并确认首页、`/api/history` 与 `/api/settings` 均返回 200，现有两条档案可正常从 SQLite 读取。后续需把“必须通过本地服务打开”做成明确启动入口与断线提示，避免用户误把静态页面可见当成服务已启动。
- 2026-09-03 已补本地可靠启动方案：新增可双击的 `打开玄衡.command`，它先检测 4173 服务，未启动时在终端前台运行 `server.py`，服务就绪后自动打开正确网址；终端明确提示必须保持窗口开启。正式页新增全局“本地服务未连接”提示和“重试连接”按钮，档案/定位/排盘操作在离线时会明确拦截。已验证脚本语法、页面脚本语法及首页、档案、配置接口均为 200。尝试过 macOS LaunchAgent 常驻，但因桌面目录后台访问限制退出，已完整撤销，不作为交付方案。
- 2026-09-03 用户完成一轮 V1 人工验收：A“资料录入与历法换算”全部通过；B 仅剩藏干、十神、纳音、神煞完整展示与固定样例人工核对；C 仅剩问事历史保存、重开复用与导出；D 因尚未上线暂未测试。
- 2026-09-03 用户正式选定“玄夜星图”为产品视觉方向；后续 V1 与 V2 正式页面均使用同一深墨、星图、金色强调设计系统，不再继续三套方向并行。
- 2026-09-03 版本推进策略调整：无需等待 V1 单独验收结束，先补完 V1 的 B/C 缺口，再直接实现 V2 塔罗，完成后由用户统一验收 V1+V2；D 上线与多设备适配仍保留为上线阶段验收。
- 2026-09-03 已将 `https://github.com/daman-ovo-0404/tarot-skill` 克隆至 `vendor/tarot-skill` 作为 V2 规则与抽牌参考。上游包含 78 张牌、6 种牌阵、正逆位、可复现 seed/time factor 及安全边界；正式实现应保留来源与 MIT 许可说明。
- 2026-09-03 已补齐 V1 命盘字段后端与正式展示：四柱逐柱返回并显示藏干、藏干十神和纳音，神煞由确定性结果整理成结构化列表；固定样例庚午／辛巳／庚辰／壬午已通过自动核对。
- 2026-09-03 已补齐 V1 问事持久化：新增 `question_history`，按当前档案查看历史、打开原结果、删除和导出 Markdown；相同缓存身份重新打开不请求模型。验收日志新增保存、重开和导出三项。
- 2026-09-03 已完成 V2 塔罗本地 RC：后端接入 78 张牌、6 种牌阵、正逆位、seed/time factor、历史和 Markdown 导出；模型不可用时提供明确标注的本地结构化解读，并缓存已生成结果。
- V2 正式前端已使用“玄夜星图”设计系统接入：首页与全局体系切换可进入塔罗，抽牌先展示牌背并逐张翻开，全部揭示后才请求/显示解读；支持牌阵历史重开、删除和导出。
- 已新增 `ACCEPTANCE_CHECKLIST.md` 作为 V1+V2 联合验收清单，新增 `THIRD_PARTY_NOTICES.md` 记录上游来源与许可状态。当前上游 Tarot README 声称 MIT，但取得提交没有独立 `LICENSE` 文件，正式公开发布前需进一步确认。
- 2026-09-03 自动验证结果：玄衡新增 5 项后端测试、上游八字 11 项测试、Python 编译和页面内联 JavaScript 解析全部通过；HTTP 冒烟测试确认首页、玄夜样式、塔罗牌库、抽牌、历史导出和删除接口可用，牌库为 78 张唯一牌、6 种牌阵。
- 2026-09-03 用户验收发现塔罗在问题为空时仍可抽牌并调用模型解读。已将“明确问题”设为抽牌前置条件：输入为空时前端按钮禁用并提示，后端 `/api/tarot/draw` 同步返回 400，不写入牌面、解读或历史。由错误操作产生的唯一一条空问题记录已删除；新增空问题拒绝测试后，玄衡测试更新为 6 项全部通过。
- 2026-09-03 用户要求开始 V3 紫微斗数后端。已将 `https://github.com/FANzR-arch/Numerologist_skills` 克隆至 `vendor/Numerologist_skills`，取得提交 `ea28c3f`；上游提供规则与单项 `ziwei_verify.py`，没有完整全盘引擎，且未发现独立 LICENSE。
- 新增 `ziwei_engine.py` 确定性引擎，按上游三合派常用口径实现：命宫/身宫、十二宫及宫干、五行局、十四主星、文昌文曲/左右/魁钺/禄羊陀/火铃/空劫/天马、生年四化、命主身主、庙旺状态、十二段大限、六个核心宫的三方四正证据和有限格局结构检测。格局检测只返回名称与可核验依据，不在后端直接断吉凶。
- 新增紫微动态层：按目标年份返回虚岁、当前大限与完整十年、流年命宫，以及本命/大限/流年三层四化。新增 `/api/ziwei/status`、`POST /api/ziwei/chart` 和 `POST /api/ziwei/timing`。
- 紫微直接复用现有出生档案；也支持同格式直接参数。相同排盘身份与规则版本写入 `ziwei_chart_cache` 并复用，编辑或删除档案会清除关联缓存。时间不确定时拒绝生成唯一盘；真太阳时沿用档案经度和时区；23:00 后采用次日子时；闰月当前默认按原月序并在结果中显式提示。
- 紫微验收日志已预留“完成排盘、缓存复用、结构自检”三项，不记录用户出生资料。固定样例、紫微安星性质、完整结构、动态四化、缓存复用、缺失时辰拒绝、23 点换日和农历合法日期均已纳入测试。当前玄衡自动测试 12 项全部通过（含前端入口防回锁检查），HTTP 冒烟确认状态、全盘、缓存、档案复用和流年接口可用。
- 2026-09-03 用户指出后端虽已接入，但全局入口仍显示“确定性引擎待接入／紫微斗数暂未开放”。已推进 V3.1：启用全局紫微体系入口和首页模块，复用已有出生档案，接入十二宫星图、宫位点选详情、主星庙旺、辅煞、生年四化、三方四正、五行局/命主身主，以及本大限十年和本命/大限/流年三层四化切换。页面明确保留“V3 · 校盘中／仍待 30 盘对照”，不伪装为正式发布。
- V3 前端验收日志新增“查看宫位与三方四正”（权重 2）和“切换流年与三层四化”（权重 3）；只记录宫位支、层数等非敏感结果，不监控滚动。
- 2026-09-03 用户截图验收发现两个状态/视觉缺口：紫微结果已成功返回时加载提示仍常驻；八字未选档案页仍显示旧版浅灰大画布，与塔罗和紫微的玄夜星图断层。日志确认紫微排盘、缓存和结构自检均成功，因此前者定位为 CSS 的 `display` 覆盖原生 `hidden` 属性，而非后端卡住。已为紫微加载态/结果态恢复强制互斥，并把八字空状态改为与另外两套体系一致的深墨、金色强调工作台；自动测试加入对应防回归检查。
- 2026-09-03 用户截图验收发现全局体系切换器在四柱、塔罗和紫微页面的横向位置不一致，四柱空盘页偏移最明显。访问日志确认同期八字页面、塔罗牌库、紫微排盘与运限接口均正常返回 200，故障与路由/后端无关；根因是顶栏使用 `flex + space-between`，不同页面右侧控件数量和宽度会反向改变切换器位置。桌面顶栏已改为共享五列固定轨道，体系切换器固定在第二列并统一宽度，三套体系不再各自漂移；窄屏继续沿用折行布局。
- 2026-09-03 主页视觉复核后继续收敛玄夜星图样式：底部“从确定的出生时空开始”由残留的浅色大卡改为深墨、金色主操作和统一 12px 圆角，并缩短与功能卡片之间的间距；首页左上星图坐标组的编号、北斗与说明各提高两个字号层级并增强对比度。全局体系切换器的共享桌面轨道整体左移 40px，四柱、塔罗和紫微仍保持完全同轨，不恢复逐页定位。
- 2026-09-03 已修复四柱页旧 `.shell` 造成的整页白框与顶栏下移：保留其内容区网格职责，但清除 `12px` 外边距、白色半透明边框、28px 圆角、阴影和模糊，并把顶层宽度与高度恢复为普通页面坐标；网格首行改为随共享顶栏实际高度自适应。四柱、塔罗、紫微现从同一页面原点开始，未再使用单页按钮位移补丁。

### 尚未完成或未通过验收

- V1 命盘完整字段、问事历史/重开/导出已实现，但仍等待用户联合人工验收。
- V2 塔罗已实现本地 RC，但六种牌阵、翻牌节奏、模型解读、本地降级、历史重开和导出仍等待用户联合人工验收。
- V3.0 紫微后端尚未达到正式发布门槛：需要至少 30 份权威软件逐宫对照，补足五行局、男女顺逆、子时、闰月与真太阳时边界样例；当前闰月只有“按原月序”默认口径，尚未提供口径切换。
- V3.1 十二宫校盘前端已接入，但尚未完成用户人工验收和权威软件逐宫对照。
- V3.2 的 LLM 宫位解释层尚未接入；V3.3 的确定性大限/流年三层四化已先行接入当前校盘页，后续仍需确认最终信息密度与交互。
- D 类异常和桌面/平板/手机适配尚未上线测试。
- 当前数据只保存在本机，不支持账户、跨设备或线上数据库。
- `index.html + server.py` 是当前可用实现；Sites 工程的 `app/page.tsx` 仍是占位页，尚未接入真实产品。
- `.openai/hosting.json` 当前没有 D1/R2 配置，正式线上持久化尚未建立。
- 当前不能把 GitHub Pages 当成完整部署方案：纯静态托管无法运行 Python 排盘服务、保护模型密钥或保存云端历史。
- 本地版依赖 `server.py` 持续运行；应通过 `打开玄衡.command` 进入并保持其终端窗口开启。直接双击 `index.html` 仍只能保证界面可见，但现在页面会明确显示离线状态并阻止需要后端的操作。
- 紫微斗数已接入当前本地项目的确定性后端与校盘前端；正式发布仍受 30 盘校对、闰月口径和上游许可确认约束。

## 5. V1.0 剩余用户验收清单

本清单只保留尚无明确成功日志，或必须依靠人眼、手感和内容判断的项目。已经有强语义成功事件的基础操作不再重复列出。

- [ ] 人工确认命盘结果页完整展示四柱、藏干、十神、纳音、五行、大运、流年和神煞，且与固定样例一致（功能已实现，自动测试通过）。
- [ ] 人工确认问事结果自动进入历史；重新打开同一次问事不重复调用模型或产生不同答案（功能已实现，自动测试通过）。
- [ ] 人工确认问事历史可按档案查看、重新打开，并导出完整 Markdown（功能已实现，接口冒烟通过）。
- [ ] API失败、地图失败、排盘失败和模型失败时均有明确错误、重试与不丢数据的降级状态。
- [ ] 手机或窄窗口下没有内容被截断、按钮不可见或页面无法滚动。

### 推荐固定验收样例

- 普通阳历：1990-05-15 12:00，男；应为庚午／辛巳／庚辰／壬午。
- 农历对应：1990 年四月廿一，午时，应与上述阳历盘一致。
- 早子时：00:30；日柱保持当天。
- 夜子时：23:30；按现有脚本口径使用次日日柱。
- 时间未知：年月日柱保留，时柱显示未知。
- 真太阳时：使用一个非东经 120° 的城市，确认校正分钟和跨时辰边界。

## 6. V1/V2 视觉统一待办清单

### 已完成的方向探索

- [x] 使用同一套 10 页信息架构制作三套可切换、可交互的视觉稿，避免因内容差异影响风格比较。
- [x] 在三套视觉稿中统一产品名称、主导航、档案/出生资料/命盘报告/岁运时间轴/问一件事等术语。
- [x] 为三套视觉稿建立一致的按钮层级、服务状态、空/使用态骨架和同一套线性图标语言。
- [x] 用户选择“玄夜星图”为正式主视觉方向。

### P0：先统一产品骨架

- [x] 固定全站顶部导航的体系入口、功能导航和档案入口，并为八字、塔罗和 V3 紫微预留统一体系切换。
- [x] 固定四柱、塔罗、紫微体系切换器的桌面横向轨道和控件宽度，避免因各页右侧操作数量不同产生偏移。
- [x] 清除四柱页旧 `.shell` 的外边框、12px 外边距、圆角与独立顶层网格偏移，使三套体系顶栏共享同一页面坐标系。
- [ ] 统一“当前档案”上下文：命盘、岁运、问事均以同一轻量组件显示档案名，并支持切换和刷新恢复。
- [x] 为正式 V1/V2 页面套用玄夜星图颜色、卡片、页面宽度、页头和内容节奏；塔罗空态、翻牌态与解读态使用同一工作台骨架。
- [x] 问事空态与结果态保留同一档案名、模式、输入和历史入口，不再切换成另一套页面气质。
- [ ] 建立术语表并全站替换：统一“档案/资料/命盘/报告”“我的档案/用户档案”“新增/新建/建立”等名称。
- [ ] 把 API 配置、API 已接入、研判服务正常合并为同一个全局服务状态与入口。

### P1：统一视觉语言与交互状态

- [x] 玄夜星图正式层固定三种字体角色：衬线标题/命理符号、无衬线正文/输入、等宽编号/眉题。
- [ ] 提高 7—9px 小字的最小字号和对比度，避免把必要信息做成装饰性纹理。
- [x] 新增玄夜星图正式样式层，统一主要页面宽度、顶栏、边框、阴影、圆角和金色强调语义。
- [x] 命盘补充字段、问事历史和塔罗工作台均使用同一深墨卡片语言，避免新增模块再次分化。
- [ ] 统一按钮层级：主操作、次操作、文字操作、危险操作、选中和禁用状态各保留一种明确样式。
- [ ] 统一状态色语义：黑色、淡紫、绿色分别固定为主操作/选中提示/服务成功，不再交叉使用。
- [ ] 用同一套图标替换太阳、月亮、田、男女、无穷号、梅花和多种箭头等混合字符，统一尺寸、描边与基线。
- [ ] 保持编辑来源上下文：从命盘点“修改信息”时，弹窗关闭后回到原命盘；从档案编辑时仍回到档案。
- [ ] 清理面向用户的实现术语：`PYTHON ENGINE`、`LOCAL FIRST`、`BAZI STUDY`、`交互前端原型`改为统一品牌语言或移入技术说明。
- [ ] 统一页脚、传统文化说明、隐私说明和免责声明的出现位置与表达。

### P2：收尾与视觉回归

- [ ] 完成设计理念页正式内容；合缘页只保留明确的待开放说明，不展示内部开发备注或“等待共同确定”等占位文案。
- [ ] 桌面、平板、手机分别核对导航折叠、卡片重排、弹窗滚动、地图全屏和问事输入区。
- [ ] 为主页、空状态、结果状态、档案页和编辑弹窗建立固定尺寸截图基线，后续修改进行视觉回归对比。

## 7. 下一步推进顺序

1. **人工验收 V3.1 前端闭环**：从体系切换进入紫微，验证已有档案选择、十二宫方向、宫位详情和本大限十年/三层四化切换；错误与布局反馈先在校盘页修复。
2. **V3.0 校盘扩充**：用至少 30 份权威软件盘逐宫对照，并优先覆盖五种五行局、男女顺逆、23 点换日、闰月和真太阳时边界；发现口径差异时先记录规则，不用模型修正计算。
3. **确定闰月产品口径**：在“原月序”和其他常用闰月处理之间做产品选择，再实现可切换规则与回归样例。
4. **进行本地 V1+V2 联合验收并修复反馈**：按 `ACCEPTANCE_CHECKLIST.md` 验收已有功能，同时不让 V3 开发破坏现有链路。
5. **完成 V3.2 解释层**：基于后端证据做宫位与三方四正解释；AI 只解释结构化盘面，不参与修正排盘。
6. **统一正式工程并上线**：迁移真实功能、建立线上数据库与服务端配置，再执行 D 类异常与多设备验收。
7. **处理第三方许可确认**：公开发布前确认 Tarot 与 Numerologist Skills 上游缺失独立许可证文件的问题，并保留来源说明。

## 8. 近期决策提醒

- V1/V2 仍等待联合验收；用户已决定先并行推进 V3 紫微后端，不能把 V1/V2 自动视为已验收。
- 紫微不能只把 SKILL 文本交给模型；必须先实现并测试确定性排盘引擎。
- 任何上线方案都必须同时覆盖：前端、Python/等价后端、数据库、模型密钥保护。只部署静态页面不算产品上线。
- GitHub 可作为源码版本库和更新协作渠道，但当前架构不能直接用 GitHub Pages 作为正式站点；上线必须把 Python 排盘能力迁移或部署为可访问的服务，并配置持久化数据库与服务端密钥。
